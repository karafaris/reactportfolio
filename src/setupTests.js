// Importing '@testing-library/jest-dom' to add custom Jest matchers for DOM assertions.
// This library enhances your testing capabilities for DOM elements.

// jest-dom adds custom jest matchers for asserting on DOM nodes.
// It allows you to perform more precise assertions on DOM elements.
// For example, you can use expect(element).toHaveTextContent(/react/i)
// to check if an element contains text matching the regular expression /react/i.

// To learn more about how to use jest-dom, visit the documentation at:
// https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';
