// Import the 'react' module
import React from 'react';

// Define the Home functional component
const Home = () => {
  return (
    <div>
      {/* Render a simple text within a <div> */}
      hi, I am REACT
    </div>
  );
};

// Export the Home component for use in other parts of the application
export default Home;
